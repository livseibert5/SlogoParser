/**
 * User types '50 fd' in the command window and sees an error message that the command was not formatted correctly
 */

BackEndExternal.parse(command);

//if parsor determines that there's an error, then
        
FrontEndInternal.createErrorBox();