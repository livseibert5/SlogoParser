/**
 * Use case: User puts a set of commands inside a repeat loop.
 */

BackEndExternal.parse(command)
// Parser will use Loop Command class to take in a repeat command's number of iterations and list of commands to run.
// repeat command will execute the list of commands in a loop that runs the specified number of times