/**
 * Use case: Use turtle to draw a "color-changing" line.
 */
BackEndExternal.parse(command)
// Back end internal calls setColor with different r g b values to change the color of the line at different intervals
    BackEndInternal.setColor(int r, int g, int b)
    BackEndInternal.setColor(int r, int g, int b)
    BackEndInternal.setColor(int r, int g, int b)
