/**
 * Use case: User enters a math equation and backend returns the correct value
 */
BackEndExternal.parse(command);
// Parser determines that the command is an equation and sends it to the Math command class.
BackEndInternal.add(int a, int b);
// Controller gets the result from the parser.
BackEndExternal.getParserResult();
FrontEndInternal.displayResult();