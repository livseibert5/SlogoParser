/**
 * The user sets the pen's color using the UI so subsequent lines drawn when the turtle moves use that color.
 */
// On dropdown selection, the following method is called with the appropriate color codes.
 FrontEndExternal.changePenColor(int r, int g, int b);